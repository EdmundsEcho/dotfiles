return {
    "ellisonleao/glow.nvim",
    ft = { "markdown" },
    config = true,
    cmd = "Glow",
}
-- return {
--     "ellisonleao/glow.nvim",
--     enabled = false,
--     opts = {
--         glow_path = "~/go/bin", -- will be filled automatically with your glow bin in $PATH, if any
--         border = "shadow", -- floating window border config
--         pager = false,
--         height = 100,
--         width_ratio = 0.7, -- maximum width of the Glow window compared to the nvim window size (overrides `width`)
--         height_ratio = 0.7,
--         style = "dark",
--         width = 120,
--     },
--     config = true,
--     cmd = "Glow",
-- }
